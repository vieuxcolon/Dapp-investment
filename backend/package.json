{
  "name": "dapp-investment-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Hardhat backend for Investment DAO DApp",
  "scripts": {
    "compile": "hardhat compile",
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "test": "hardhat test"
  },
  "devDependencies": {
    "hardhat": "2.22.3",
    "@nomicfoundation/hardhat-toolbox": "3.0.0"
  },
  "dependencies": {
    "ethers": "6.14.0",
    "@openzeppelin/contracts": "4.9.3",
    "dotenv": "16.3.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
